DATABASE_URL="file:./dev.db"
ADMIN_PASSWORD_HASH=
SESSION_SECRET=
# Optional: set BOTH to use Supabase instead of local SQLite.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# Local uploads; not used with Supabase.
UPLOAD_DIR="data/uploads"
# Optional public contact details. Leave empty until the operator supplies real details.
NEXT_PUBLIC_CONTACT_PHONE=
NEXT_PUBLIC_CONTACT_EMAIL=
